@model IEnumerable<string>
<section id="originalrecipe">
    <ol>
        @if (Model != null)
        {
            foreach (string item in Model)
            {
            <li>
                @item
            </li>
            }
        }
    </ol>
</section>